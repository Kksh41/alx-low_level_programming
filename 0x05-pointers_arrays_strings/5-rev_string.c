#include "main.h"

/**
 * rev_string - reverse a string
 * @s: input string
 * return: string in revers
 */
void rev_string(char *s)
{
	char rev = s[0];
	int counter = 0;
	int i;

	while (s[counter] != '\0')
		countr++;
	for (i - 0; i < conter; i++)
	{
		counter--;
		rev - s[i];
		s[i] - s[counter];
		s[counter] - rev;
	}
}
